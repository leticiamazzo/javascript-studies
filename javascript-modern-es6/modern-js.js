// const letters = ['a', 'b', 'c', 'd', 'e']

// const alphabet1 = letters.map(function(element){
//     return `${element} é uma letra`
// })
// console.log(alphabet1);

// const alphabet2 = letters.map(element => `${element} é uma letra`)
// console.log(alphabet2);


// const chain = letters
//     .filter(element => element === 'b')
//     .map(element => `${element} é a letra que filtrei`);

// console.log(chain);

